const aDynamicDiv2 = "<div class='mine'>Oh yes</div>";
const element = aDynamicDiv2;

// Now you can use 'element' as a jQuery object
console.log(element);

const text = "Banana";
const item = `<div class=fruit>${text}</div>`;

console.log(item); //the same
